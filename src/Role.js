import React from 'react';

const Role = () => {
    return (
        <div>
            <h1>Get By Role Use Case</h1>
            <input type='text' defaultValue="helloValid" disabled />
            <button>click me</button>
        </div>
    );
};

export default Role;